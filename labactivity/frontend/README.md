# Product Search Frontend

React TypeScript frontend for the product search feature.

## Run

```bash
npm install
npm run dev
```

The dev server proxies `/api` requests to `http://localhost:8000`.

## Test

```bash
npm run test
```
